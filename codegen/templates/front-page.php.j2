<?php
/**
 * The Front Page template
 *
 * Displays the AST-derived SaaS Hero & Feature Section design directly
 * as the site's primary homepage upon theme activation.
 *
 * @package {{ theme_slug | default('figma-theme') }}
 */

get_header();
?>

<main id="primary" class="site-main {{ main_class | default('site-front-page') }} bg-slate-950 text-slate-100 min-h-screen">
{{ body_content }}
</main><!-- #primary -->

<?php
get_footer();
